<Window x:Class="LOR_FBA.ExportPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LOR_FBA"
             mc:Ignorable="d" 
             Width="300">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Categories to Export:"
                   FontWeight="Bold"
                   Margin="10,5,0,5"/>

        <ListBox x:Name="categoryListBox" Grid.Row="1" 
                 Margin="10,10,10,10"
                 ItemsSource="{Binding CategoryList}"
                 SelectionMode="Multiple"
                 />

        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10,0,10">

        <Button Content="Export" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,5,0" Width="100" 
                    Height="30" Click="Button_Click"/>

            <Button Content="Cancel" 
                    HorizontalAlignment="Center" 
                    Margin="5,0,0,0" Width="100" 
                    Height="30" IsCancel="True"/>

        </StackPanel>
    </Grid>
</Window>
